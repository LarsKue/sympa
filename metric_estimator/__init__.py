from .core import MetricEstimator
from .data import MetricDistanceSet



def main(ndim, train_path="train", val_path="validate", overwrite=False):

    return MetricEstimator(model, manifold, optimizer, loss, train_loader, val_loader, batch_shape)
